{% extends 'base.html.twig' %}

{% block title %}Edit Event
{% endblock %}

{% block body %}
	<div class="container">
		<h1>Modifier l'événement</h1>
		{{ include('event/_form.html.twig', {'button_label': 'Update'}) }}


		<a href="{{ path('app_event_index') }}" class="btn btn-violet">Retour à la liste</a>

		{{ include('event/_delete_form.html.twig') }}
	</div>
{% endblock %}
